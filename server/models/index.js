const Company = require('./Company');
const User = require('./User');
const Location = require('./Location');
const Post = require('./Post');
const Tag = require('./Tag');

module.exports = { Company, User, Post, Location, Tag };
